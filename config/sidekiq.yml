:concurrency: 5
:timeout: 25
:verbose: false
:queues:
  - default
  - mailers
  - [urgent, 2]

:schedule:
  check_all_urls:
    cron: '0 * * * *'  # Every hour (smart scheduling decides which URLs to check)
    class: CheckAllUrlsJob
    queue: default
