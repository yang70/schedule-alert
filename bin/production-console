#!/usr/bin/env bash
# Script to run Rails console in production with environment variables loaded

cd "$(dirname "$0")/.."

if [ -f .env.production ]; then
  set -a
  source .env.production
  set +a
fi

export RAILS_ENV=production
bundle exec rails console
